# web-storefront

Customer-facing app: catalog, smart-home configurator, cart, Customer Portal
(Dashboard → Configurations → Order Detail → Invoice/Appointment → Completion/Support → Personal Profile).

Consumes: `catalog`, `orders`, `finance` (customer-facing subset only — see
docs/module-workflows-screen-spec.md for what's hidden from this view).
